(function() {
    'use strict';

    var root = this;

    // define object literal of module configuration
    root.define({
	    navigation: "APP:START",
	    modal: "APP:START",
	    account: "APP:START"
	});

}).call( this );